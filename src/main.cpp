#include "principal.h"

int main(){

    Principal objeto;

    objeto.executar();

    /*
        Na classe principal o professor chama o método para executar dentro da construtora, por isso
        no código dele, não é necessário chamar o método executar na main, apenas declarando o objeto.
    */
   
   
    return 0;
}